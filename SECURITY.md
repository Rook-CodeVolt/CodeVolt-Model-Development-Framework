# Security Policy

Do not open a public issue for vulnerabilities, leaked credentials, private datasets, unsafe model artifacts, or prompt/data content that could harm people. Use GitHub's private vulnerability reporting for this repository when available.

The `main` branch is the currently supported development line. This early release is experimental and must not be used as an unattended production model-promotion system.
